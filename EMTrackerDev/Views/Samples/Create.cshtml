@model EMTrackerDev.Models.Sample

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Sample</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AnalysisId" class="control-label"></label>
                <select asp-for="AnalysisId" class="form-control" asp-items="ViewBag.AnalysisId"></select>
            </div>
               <div class="form-group">
                <label asp-for="LocationCodeId" class="control-label"></label>
                <select asp-for="LocationCodeId" class="form-control" asp-items="ViewBag.LocationCodeId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
